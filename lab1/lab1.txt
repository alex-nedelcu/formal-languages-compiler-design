************** p1) Compute min of 3 integer numbers

	START

		define integer first;
		define integer second;
		define integer third;

		read(first);
		read(second);
		read(third);

		define integer min = first;

		if (second < min) {
			min = second;
		}

		if (third < min) {
			min = third;
		}

		diplay("minimum-value:");
		display(min);

	END


************** p2) Compute largest divisor of a natural number that is less than the number itself

	START

		define integer number;
		define integer divisor;

		read(number);

		loop (iterator=divisor, start=number-1, step=-1, until=1) {
			if (number % divisor == 0) {
				stop;
			}
		}

		display("divisor-is:");
		display(divisor);

	END


************** p3) Compute largest value from array

	START

		define integer[100] array;
		define integer index;
		define integer max;

		loop (iterator=index, start=0, step=1, until=99) {
			read(array[index]);

			if (index == 0) {
				max = array[index];
			} else if (array[index] > max) {
				max = array[index];
			}
		}

		display("maximum-value-from-array-is:");
		display(max);

	END


************** p1err)
	
	START

		define bool flag;
		define string s1;
		define string 2s;

		read(s1);
		read(2s);

		if (s1 != 2s) {
			display(s1);
			display("and~);
			display(2s);
			display("are-different");
		}

	END
