START

    define integer first;
    define integer first;
    define integer third;

    read(first);
    second = P#1#;
    read(third);

    define integer min= first;

    if (
            second
            ==
            min
     ) {
        min=second;
    }

    if (third <=
     min) {
        min = third;
    }

    display("minimum_value_is_");
    display(min);

END
